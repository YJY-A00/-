## ⚠️ 遇到的问题及解决方案

在搭建过程中，我遇到了以下主要问题。这些问题没有一个是我在教程里提前看到的，全部是在实际操作中“卡住”后，自己一步步排查并解决的。

---

### 问题一：Workbench 连接超时

| 项目 | 内容 |
| :--- | :--- |
| **现象** | 阿里云 Workbench 报 `SocketTimeoutException`，无法连接服务器 |
| **排查过程** | 检查本地网络、重启服务器、更换连接方式 |
| **解决方案** | 改用 **VNC（管理终端）** 连接，绕过公网 SSH，成功登录 |

---

### 问题二：宝塔面板安装卡死

| 项目 | 内容 |
| :--- | :--- |
| **现象** | 安装 PHP 时 VNC 无响应，系统报 `kcompactd0 blocked for more than 120 seconds` |
| **排查过程** | 执行 `top` / `free -h` 查看资源，发现内存耗尽 |
| **解决方案** | 升级服务器内存至 1 GiB + 改用 **“极速安装”**（预编译包，避免编译开销） |

---

### 问题三：Docker WordPress 端口访问

| 项目 | 内容 |
| :--- | :--- |
| **现象** | `http://yjyzy.lol` 无法访问，但 `http://47.237.144.216:18040` 可以 |
| **排查过程** | 确认 Docker 容器正常运行，端口映射正确 |
| **解决方案** | 继续使用 IP+端口 方式访问，等域名完全生效后再切换 |

---

### 问题四：WordPress 安装报“域名已存在”

| 项目 | 内容 |
| :--- | :--- |
| **现象** | 在 Docker 应用中安装 WordPress 时提示“域名已存在，请勿重复添加” |
| **排查过程** | 检查宝塔“网站”列表，发现之前创建过一个同名空站点 |
| **解决方案** | 删除旧站点，重新安装 Docker 应用，成功部署 |

---

### 问题五：VNC 登录超时

| 项目 | 内容 |
| :--- | :--- |
| **现象** | 在 `Password:` 提示后输入密码，60 秒后提示 `Login timed out` |
| **排查过程** | 确认输入内容，发现误将阿里云账号名当作密码输入 |
| **解决方案** | 明确：`root` 为用户名，密码为购买服务器时设置的密码 |

---

### 问题六：服务器彻底卡死，VNC 也连不上

| 项目 | 内容 |
| :--- | :--- |
| **现象** | VNC 黑屏，按任意键无反应，所有操作无效 |
| **排查过程** | 判断为系统假死，非网络问题 |
| **解决方案** | 在阿里云控制台执行 **“重启”** 或 **“停止再启动”**，强制恢复 |